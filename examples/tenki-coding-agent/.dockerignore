# Never bake local credentials into the image.
.env*
!.env.example

# Installed inside the image; copying the host's would overwrite it
# with platform-specific binaries.
node_modules

# Compiled inside the image; a stale host build could leave orphan files.
dist
